[package]
name = "bookmark_rust"
version = "0.1.0"
edition = "2024"
[dependencies]
actix-web = "4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.7", features = ["serde", "v4", "rng"] }
chrono = { version = "0.4", features = ["serde"] }
rand = "0.8"

